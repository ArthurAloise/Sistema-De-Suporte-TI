APP_NAME="Sistema de Suporte TI"
APP_ENV=production
APP_DEBUG=false
APP_URL=https://meuchamado.gisellearquitetura3d.com.br

APP_KEY=base64:r9sbsvuctcFpBqPVK1Kq0eidNQj5jVWEHUfXfnjZgPw=

LOG_CHANNEL=stack
LOG_LEVEL=warning

# --- Banco (HostGator) ---
DB_CONNECTION=mysql
DB_HOST=ns624.hostgator.com.br
DB_PORT=3306
DB_DATABASE=perici73_meuchamado
DB_USERNAME=perici73_stanley
DB_PASSWORD="l)y2zNz6{+ac"

# --- Sessão/Cache (simples p/ compartilhado) ---
SESSION_DRIVER=file
SESSION_LIFETIME=120
CACHE_DRIVER=file
FILESYSTEM_DISK=public
QUEUE_CONNECTION=sync

# --- E-mail (Titan) ---
MAIL_MAILER=smtp
MAIL_HOST=smtp.titan.email
MAIL_PORT=465
MAIL_ENCRYPTION=ssl
MAIL_USERNAME=suporte@meuchamado.gisellearquitetura3d.com.br
MAIL_PASSWORD="8:Ry3[OR*(4,H$M"
MAIL_FROM_ADDRESS="suporte@meuchamado.gisellearquitetura3d.com.br"
MAIL_FROM_NAME="Prefeitura de Ariquemes - Suporte TI"

# (deixe o resto como está)

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https
PUSHER_APP_CLUSTER=mt1

VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_HOST="${PUSHER_HOST}"
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"

ORG_NAME="Prefeitura de Ariquemes"
ORG_UNIT="Suporte TI"
MAIL_FROM_NAME="${ORG_NAME} - ${ORG_UNIT}"
